{% extends "@admin/index.html.twig" %}

{% import "@admin/macros.html.twig" as macro %}

{% block body %}
    {{ macro.breadcrumb() }}

    <table class="table table-striped table-sm">
        <thead>
        <tr>
            <th>
                <a href="{{ path('admin_countries_add') }}" class="btn-sm btn-primary">
                    <i class="fas fa-plus"></i>
                </a>
            </th>
            <th>Pays</th>
            <th>Abréviation</th>
            <th>Commentaire</th>
        </tr>
        </thead>
        <tbody>
        {% for item in items %}
            <tr>
                <td>
                    <a href="{{ path('admin_countries_edit', {id: item.id}) }}" class="btn-sm btn-primary">
                        <i class="fas fa-pen"></i>
                    </a>
                </td>
                <td>{{ item.name }}</td>
                <td>{{ item.abbr }}</td>
                <td>{{ item.comment }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="text-right">
        <span class="badge badge-light">{{ items|length }} pays</span>
    </div>
{% endblock %}

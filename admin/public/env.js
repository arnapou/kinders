
var API_HOSTNAME = 'kinders.arnapou.local';

window.env = {
    // This option can be retrieved in "src/index.js" with "window.env.API_URL".
    API_BASE_URL:    'http://' + API_HOSTNAME + '/api',
    API_LOGIN_CHECK: 'http://' + API_HOSTNAME + '/login_check'
};